export class Learn {

    numberValue: number;
    stringValue: string;



        constructor(initialNumber: number, initialString: string) {
            this.numberValue = initialNumber;
            this.stringValue = initialString;
        }
        

 name() {
        console.log( "This is a function inside the Learn class.");

}
}

